import { RequestRidePipe } from './request-ride.pipe';

describe('RequestRidePipe', () => {
  it('create an instance', () => {
    const pipe = new RequestRidePipe();
    expect(pipe).toBeTruthy();
  });
});
